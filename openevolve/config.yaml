max_iterations: 50
llm:
  models:
    - name: "claude-haiku-4-5-20251001-v1:0"
      weight: 1.0
  api_base: "https://ai-gateway.andrew.cmu.edu"
prompt:
  system_message: |
    Evolve `_device_allocate_policy` to maximize `throughput / (1 + hbf_write_rate_MBps)`.
